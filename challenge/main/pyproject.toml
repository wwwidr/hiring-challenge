[project]
name = "contact-finder-stageb"
version = "0.1.0"
description = "Stage B contact finder pipeline using mocked providers"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "pydantic>=2.8,<3",
]

[tool.uv]
package = false
